import { ScheduleCalendar } from "@/components/shared/ScheduleCalendar";

// Was a near-copy of the tutor's, differing only in margins and font weights.
// Now the same component, which is also the one that was better looked after.
export function CounselorCalendar() {
  return <ScheduleCalendar role="counselor" />;
}
