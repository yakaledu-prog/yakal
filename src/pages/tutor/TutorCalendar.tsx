import { ScheduleCalendar } from "@/components/shared/ScheduleCalendar";

// The calendar itself is shared with the counsellor's. See ScheduleCalendar.
export function TutorCalendar() {
  return <ScheduleCalendar role="tutor" />;
}
