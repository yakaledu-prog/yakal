import { chromium } from 'playwright';
import { createClient } from '@supabase/supabase-js';
const BASE=process.env.BASE||'http://localhost:5173';
const SERVICE='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU';
const db=createClient('http://127.0.0.1:54321',SERVICE,{auth:{persistSession:false}});
const pass=(s,ok,d='')=>console.log(`${ok?'PASS':'FAIL'}  ${s}${d?'  -> '+d:''}`);

const TMP='link.child@yakal.test';
const {data:list}=await db.auth.admin.listUsers({page:1,perPage:200});
const ex=list.users.find(u=>u.email===TMP); if(ex) await db.auth.admin.deleteUser(ex.id);
const {data:created}=await db.auth.admin.createUser({email:TMP,password:'demo123',email_confirm:true,user_metadata:{full_name:'Link Child',role:'student'}});
await db.from('profiles').update({is_onboarded:true,status:'active'}).eq('id',created.user.id);
const {data:parent}=await db.from('profiles').select('id,full_name').eq('email','parent@yakal.com').single();
await db.from('parent_student_links').insert({parent_id:parent.id,student_id:created.user.id,status:'pending'});
await db.from('notifications').insert({user_id:created.user.id,type:'parent_link',title:'Parent link request',message:`${parent.full_name} has asked to link to your account as your parent.`,link:'/student/notifications'});

const b=await chromium.launch();
const p=await (await b.newContext({viewport:{width:1440,height:900}})).newPage();
const errs=[]; p.on('pageerror',e=>errs.push(e.message));
await p.goto(`${BASE}/login`,{waitUntil:'domcontentloaded'});
await p.locator('input[type="email"]').first().fill(TMP);
await p.locator('input[type="password"]').first().fill('demo123');
await p.locator('form button[type="submit"]').first().click();
await p.waitForURL(u=>!u.pathname.includes('/login'),{timeout:20000});
await p.goto(`${BASE}/student/notifications`,{waitUntil:'domcontentloaded'});
await p.waitForTimeout(4000);
await p.getByText('Parent link request').first().click();
await p.waitForTimeout(2500);
pass('accept button shown', await p.getByRole('button',{name:/^accept$/i}).isVisible().catch(()=>false));
pass('decline button shown', await p.getByRole('button',{name:/^decline$/i}).isVisible().catch(()=>false));
await p.getByRole('button',{name:/^accept$/i}).click();
await p.waitForTimeout(3500);
const {data:link}=await db.from('parent_student_links').select('status').eq('student_id',created.user.id).maybeSingle();
pass('accepting activates the link', link?.status==='active', link?.status);
const {data:pn}=await db.from('notifications').select('title').eq('user_id',parent.id).order('created_at',{ascending:false}).limit(1);
pass('parent is told', /accepted/i.test(pn?.[0]?.title??''), pn?.[0]?.title);
pass('no page errors', errs.length===0, errs[0]?.slice(0,110)??'');
await b.close();
await db.auth.admin.deleteUser(created.user.id);
